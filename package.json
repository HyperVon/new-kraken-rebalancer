{
  "name": "kraken-rebalancer",
  "version": "1.0.0",
  "description": "Kraken Rebalancer developer tools and harness utilities",
  "private": true,
  "scripts": {
    "lint:md": "markdownlint '**/*.md' --ignore node_modules --ignore '**/build/**' --ignore '.agents/skills/**/examples/**'",
    "lint:kt": "./gradlew spotlessCheck",
    "test:all": "./gradlew check",
    "test:frontend": "./gradlew :frontend-js:jsBrowserTest",
    "setup:playwright": "npx playwright install chromium"
  },
  "devDependencies": {
    "@modelcontextprotocol/server-memory": "^2026.8.31",
    "@playwright/mcp": "^0.0.80",
    "markdownlint-cli": "^0.49.1",
    "playwright": "^1.62.1"
  }
}
